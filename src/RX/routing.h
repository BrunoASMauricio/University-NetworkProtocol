#ifndef RX_ROUTING
#define RX_ROUTING

// In terms of probability of error, 1 is the worst
#define WORST_QUALITY 1

void PB_RX(in_message* msg);

void PR_RX(in_message* msg);

void PC_RX(in_message* msg);



#endif
